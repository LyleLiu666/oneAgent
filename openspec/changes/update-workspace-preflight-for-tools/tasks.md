## 1. Implementation
- [x] 1.1 Add spec deltas for workspace preflight when tools enabled
- [x] 1.2 Backend: fail-fast when tools are enabled but workspaceRoot is missing
- [x] 1.3 Backend tests: assert LLM is not called and error is actionable
- [x] 1.4 Run `openspec validate update-workspace-preflight-for-tools --strict --no-interactive`
- [x] 1.5 Run `cd backend && go test ./...`
