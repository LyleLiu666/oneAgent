## 1. Implementation
- [ ] 1.1 定义 `requires/install` frontmatter schema（文档 + 测试）
- [ ] 1.2 解析并入库到 `skill.Skill`（discover/parse）
- [ ] 1.3 实现 eligibility 计算（OS/bins/env）
- [ ] 1.4 自动推荐/子 agent 注入仅使用 eligible skills
- [ ] 1.5 新增 `oneagent skills status`/`check` 命令（含 --json/--workspace）
- [ ] 1.6 补齐单测与 CI 验证（go test + windows cross-build）

## 2. Docs
- [ ] 2.1 更新内置 `create-skill` 指南（可选字段说明）
