## 关键约束（必须遵守）
- 不要输出 `<![CDATA[ ... ]]>`（也不要把 `<![CDATA[` 当作普通文本写进字段）；避免未闭合导致工具调用失败。
- 写文件用 `write_file`，改文件用 `edit`/`edit_v2`；避免使用bash修改文件。
- 避免单次输出过长：编辑/写入内容建议分段（每段 ≤3000 字）多次调用。
- 避免把大文件一次性读/贴到上下文：优先用 `rg` 定位，再用 `read_file` 分页读取（不要用 `cat` 整个文件）。
